# AZ Youth Count - Environment Variables

# Redis Database (Vercel KV or external Redis)
REDIS_URL=redis://...
# KV_URL=redis://...  # Vercel KV uses this

# Admin Dashboard Password
ADMIN_PASSWORD=your_admin_password

# Youth Volunteer Portal Password
YOUTH_VOLUNTEER_PASSWORD=YouthBoard2026!

# reCAPTCHA (v2 checkbox)
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key

# EmailJS - For confirmation emails (browser-side)
# Sign up at https://www.emailjs.com/
# Free tier: 200 emails/month
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Resend - For reminder emails (server-side cron)
# Sign up at https://resend.com/
# Free tier: 100 emails/day, 3,000/month
RESEND_API_KEY=re_...
FROM_EMAIL=AZ Youth Count <noreply@yourdomain.com>

# Cron Security (optional)
# If set, cron endpoints will require this token
CRON_SECRET=your_random_secret

# ================================
# CRON JOB SETUP (cron-job.org)
# ================================
# 1. Go to https://cron-job.org and create a free account
# 2. Create a new cron job:
#    - URL: https://www.azyouthcount.org/api/send-reminders
#    - Schedule: Every hour (0 * * * *)
#    - Request method: GET
#    - If using CRON_SECRET, add header: Authorization: Bearer your_random_secret
# 3. The cron job will check for appointments in the next 1-2 hours and send reminders
